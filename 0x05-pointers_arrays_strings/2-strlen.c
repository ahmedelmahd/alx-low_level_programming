#include "main.h"

/**
 * int_strlen - returns the length of a string
 * @s: the string of characters
 *
 * Return: Always 0 ( Successful )
 */
int _strlen(char *s)
{
	_putchar(sizeof(*s) - 1);
	return (0);
}
